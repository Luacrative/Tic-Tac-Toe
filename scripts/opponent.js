/* TODO:
- callback on turn
winning moves for self -> block losing move -> take center -> random corner -> random square
*/

const opponent = (player, rows, cols, grid) => { 
    
};

export default opponent;